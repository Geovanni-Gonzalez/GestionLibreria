# CV_EVIDENCE — GestionLibreria

**Reinforcement for the C block** (pair: SistemaDePuntoDeVenta-ConsolApp). Unique nuance: JSON handling in C via vendored cJSON integrated into the build, alongside plain-text persistence. Cite the C block as: "console business systems in C with modular .c/.h design, JSON (cJSON) and SQLite persistence" — combining both repos in one line.
